{
  "name": "one-click-deploy",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "node --test test/**/*.test.js",
    "check:wrangler": "wrangler deploy --dry-run --outdir .wrangler/check",
    "check:bundle": "wrangler deploy --dry-run --outdir .wrangler/bundle"
  },
  "devDependencies": {
    "wrangler": "4.135.0"
  }
}
